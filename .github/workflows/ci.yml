name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

env:
  TZ: UTC
  LANG: C.UTF-8
  LC_ALL: C.UTF-8

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Set source date epoch
        run: echo "SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)" >> "$GITHUB_ENV"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install
        run: python -m pip install --upgrade pip && python -m pip install -e ".[dev]"
      - name: Ruff
        run: ruff check .
      - name: Black diagnostic
        id: black
        shell: bash
        run: |
          set +e
          black --check --diff . > black-diagnostic.txt 2>&1
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Upload Black diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: black-diagnostic
          path: black-diagnostic.txt
      - name: Enforce Black result
        shell: bash
        run: exit "${{ steps.black.outputs.exit_code }}"
      - name: Mypy
        run: mypy src
      - name: Pytest
        run: pytest
