# Debris data

Store authenticated debris records, recovery metadata, part classifications, confidence states, and physical-property inputs here. Every file requires a provenance entry in `data/source-register.csv`.
