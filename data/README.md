# Data Registry

Data are grouped by evidence stream:

- `satcom/`
- `debris/`
- `drift/`
- `search/`

Every imported dataset must include its source, retrieval date, licence, checksum, transformation history, and uncertainty notes. Raw source files must not be silently edited.
