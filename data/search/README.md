# Search data

Store survey coverage, sonar footprints, terrain classifications, sensor assumptions, and non-detection inputs here. Every file requires a provenance entry in `data/source-register.csv`.
