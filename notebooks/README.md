# Notebooks

Notebooks are for exploratory analysis and visual checks only.

Reusable calculations must be implemented in `src/mh370_inverse_inference/` and covered by tests. Notebook outputs must record the package version, input data hashes, random seed, and execution date.
